name: CI

on:
  push:
    branches: [main, production-fixes-*]
  pull_request:
    branches: [main]

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      go: ${{ steps.filter.outputs.go }}
      rust: ${{ steps.filter.outputs.rust }}
      terraform: ${{ steps.filter.outputs.terraform }}
      docker: ${{ steps.filter.outputs.docker }}
      helm: ${{ steps.filter.outputs.helm }}
      security: ${{ steps.filter.outputs.security }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            go:
              - 'control-plane/**'
              - 'control-plane/go.mod'
              - 'control-plane/go.sum'
            rust:
              - 'gateway/**'
              - 'gateway/Cargo.toml'
              - 'gateway/Cargo.lock'
              - 'rust-gateway/**'
              - 'rust-gateway/Cargo.toml'
              - 'rust-gateway/Cargo.lock'
            terraform:
              - 'infra/terraform/**'
            docker:
              - '**/Dockerfile'
            helm:
              - 'deploy/helm/**'
            security:
              - 'security/**'

  rust:
    name: Rust Build & Test
    needs: changes
    if: ${{ needs.changes.outputs.rust == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        workspace: [gateway, rust-gateway]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ${{ matrix.workspace }}
      - name: Check formatting
        run: cd ${{ matrix.workspace }} && cargo fmt --check
        continue-on-error: true
      - name: Clippy
        run: cd ${{ matrix.workspace }} && cargo clippy --all-targets --all-features
        continue-on-error: true
      - name: Build
        run: cd ${{ matrix.workspace }} && cargo build --locked
      - name: Test
        run: cd ${{ matrix.workspace }} && cargo test --locked
        continue-on-error: true
      - name: Build release
        run: cd ${{ matrix.workspace }} && cargo build --release --locked

  go:
    name: Go Build & Test
    needs: changes
    if: ${{ needs.changes.outputs.go == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Go mod tidy
        run: cd control-plane && go mod tidy
      - name: Go mod verify
        run: cd control-plane && go mod verify
      - name: Build
        run: cd control-plane && go build -o bin/appgate ./cmd/server/
      - name: Test
        run: cd control-plane && go test ./... -race -count=1 -timeout=60s
      - name: Lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=5m
          working-directory: control-plane

  terraform:
    name: Terraform Validation
    needs: changes
    if: ${{ needs.changes.outputs.terraform == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - name: Terraform fmt
        run: cd infra/terraform && terraform fmt -check -recursive
      - name: Terraform validate
        run: cd infra/terraform && terraform validate -no-color

  helm:
    name: Helm Lint
    needs: changes
    if: ${{ needs.changes.outputs.helm == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - name: Helm lint
        run: helm lint deploy/helm/appgate
      - name: Helm template
        run: helm template appgate deploy/helm/appgate > /dev/null

  security:
    name: Security Scan
    needs: [rust, go]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trivy scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          format: 'sarif'
          output: 'trivy-results.sarif'
      - name: Dependency Review
        if: github.event_name == 'pull_request'
        uses: actions/dependency-review-action@v4